# Base URL of the FastAPI backend, including the /api/v1 prefix.
VITE_API_URL=
# Google OAuth Client ID (Google Cloud Console → Credentials → OAuth 2.0 Client).
# Used by the "Continue with Google" button (Google Identity Services).
VITE_GOOGLE_CLIENT_ID=
